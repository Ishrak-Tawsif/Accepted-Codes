#include<bits/stdc++.h>
using namespace std;

int main(){

    long int n,num;

        while(cin >>n && n){
            num = (double) sqrt(n);

        if(n == (num*num))
            cout << "yes" << endl;
        else
            cout << "no" << endl;
        }
return 0;
}
